<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PrayersDhikr" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main</item>
        <item name="colorPrimaryVariant">@color/main</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/main</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.PrayersDhikr.PagiActivity" parent="Theme.PrayersDhikr">
        <item name="android:statusBarColor">@color/morning_blue</item>
        <item name="colorPrimary">@color/morning_blue</item>
        <item name="title">Dzikir Pagi</item>
    </style>

    <style name="Theme.PrayersDhikr.PetangActivity" parent="Theme.PrayersDhikr">
        <item name="android:statusBarColor">@color/afternoon_orange</item>
        <item name="colorPrimary">@color/afternoon_orange</item>
        <item name="title">Dzikir Petang</item>
    </style>
</resources>